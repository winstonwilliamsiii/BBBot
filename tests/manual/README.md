# Manual Test Scripts

These scripts are manual or ad hoc test runners that were previously stored in the repository root.

Run them from the repository root, for example:

```bash
python tests/manual/test_dcf_final_validation.py
```