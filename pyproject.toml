[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bentleybudgetbot"
version = "0.1.0"
description = "Bentley Budget Bot project"
readme = "README.md"
requires-python = ">=3.10"
