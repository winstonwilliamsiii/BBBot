[core]
# Set multiprocessing start method to 'spawn' for Windows compatibility
mp_start_method = spawn

# Database configuration (using existing MySQL database)
# Using existing Bentley_Bot database with mansa_bot schema
# Update credentials to match your existing database
sql_alchemy_conn = mysql+pymysql://root:password@localhost:3306/Bentley_Bot

# Executor configuration - use LocalExecutor with MySQL
executor = LocalExecutor

# DAGs folder
dags_folder = C:/Users/winst/BentleyBudgetBot/.vscode

# Disable loading example DAGs
load_examples = False

# Timezone configuration
default_timezone = UTC

# Security
expose_config = True

# Logging
base_log_folder = C:/Users/winst/BentleyBudgetBot/airflow_config/logs
dag_processor_manager_log_location = C:/Users/winst/BentleyBudgetBot/airflow_config/logs/dag_processor_manager/dag_processor_manager.log

[webserver]
# Web server configuration
web_server_port = 8080
expose_config = True
authenticate = False

[scheduler]
# Scheduler configuration
job_heartbeat_sec = 5
scheduler_heartbeat_sec = 5
num_runs = -1

[celery]
# If using Celery executor (not recommended for Windows development)
broker_url = redis://localhost:6379/0
result_backend = db+mysql+pymysql://root:password@localhost:3306/Bentley_Bot