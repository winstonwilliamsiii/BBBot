# Isolated dependencies for Dogon ETF model training
# Use this in a dedicated venv (e.g., .venv-dogon) to avoid
# dependency conflicts with live-trading runtime packages.

numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.3.0
scipy>=1.10.0

yfinance>=0.2.66,<1.0
websockets>=11

mlflow>=2.8.0

xgboost>=2.0.0
tensorflow>=2.15.0
keras-tcn>=3.5.6