{
  "name": "starter-template",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "build": "echo 'No build step required for static/Streamlit deployment'",
    "api:dev": "python -m uvicorn Main:app --host 0.0.0.0 --port 8000 --reload",
    "api:dev:ibkr": "python -m uvicorn Main:app --host 127.0.0.1 --port 8011 --reload"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "node-appwrite": "^14.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
