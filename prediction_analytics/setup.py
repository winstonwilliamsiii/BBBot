from setuptools import setup, find_packages

setup(
    name="prediction-analytics",
    version="1.0.0",
    description="Prediction analytics services for Bentley dashboard",
    packages=find_packages(),
    include_package_data=True,
)
